name: CI

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

jobs:
  build-and-test:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Restore
        run: dotnet restore ME_ACS_SQL_Patcher.sln

      - name: Build
        run: dotnet build ME_ACS_SQL_Patcher.sln --configuration Release --no-restore

      - name: Test
        run: dotnet test ME_ACS_SQL_Patcher.sln --configuration Release --no-build --verbosity normal
